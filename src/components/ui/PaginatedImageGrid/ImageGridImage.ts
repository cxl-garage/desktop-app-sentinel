import styled from 'styled-components';
// import { Image } from 'antd';
import { Image } from 'components/ui/Image';

export const ImageGridImage = styled(Image)`
  border-radius: 10px;
  object-fit: cover;
  aspect-ratio: 1 / 1;
  box-shadow: 0 4px 4px 0px rgb(0 0 0 / 0.25);
`;
